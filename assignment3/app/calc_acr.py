def cal_acr(width, length):
    """Calculate surface in acr

    Args:
        width (float): width dimension
        length (float): length dimension

    Returns:
        float: surface in acr
    """
    return (width*length)/45360